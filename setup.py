from setuptools import setup, find_packages

setup(name='mesoprofiles',
      version='0.0.1',
      description='A library for quantifying the mesoscale profiles of networks.',
      url='https://github.com/lenafm/mesoprofiles',
      author='Lena Mangold',
      author_email='lena.mangold@cmb.hu-berlin.org',
      packages=find_packages(),
      zip_safe=False)
